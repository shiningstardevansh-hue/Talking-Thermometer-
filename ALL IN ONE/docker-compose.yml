version: '3.8'
services:
  blind-temp-server:
    build: ./BlindTempChecker/server
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - SERIAL_PORT=none
      - SERIAL_BAUD=115200
    restart: unless-stopped
